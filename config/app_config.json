{
  "health_check_query": "SELECT COUNT(*) FROM system_health WHERE status = 'active'",
  "database": {
    "host": "localhost",
    "port": 5432,
    "name": "banking_db"
  },
  "security": {
    "max_login_attempts": 3,
    "session_timeout": 3600
  }
}
